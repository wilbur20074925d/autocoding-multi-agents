# Autocoding pipeline (optional integrations)
# Core pipeline runs in Cursor/chat; these are for Discord formatting and Google Sheets.

# Discord: 5-bot runner (Controller + Signal, Label, Critic, Judge)
discord.py>=2.0.0

# Google Sheets: append result after each prompt
gspread>=6.0.0
